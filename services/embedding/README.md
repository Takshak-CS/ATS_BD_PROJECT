# Embedding Service

Purpose:

- generate embeddings for resumes and job descriptions
- index resume vectors
- retrieve semantically similar candidates

Suggested implementation:

- `sentence-transformers`
- `FAISS` for local development
- `Milvus` later for distributed scale
